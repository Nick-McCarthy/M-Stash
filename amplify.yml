version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - echo "Creating database directory..."
        - mkdir -p database
        - echo "Running database migrations..."
        - npm run db:push
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*

